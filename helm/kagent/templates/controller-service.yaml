apiVersion: v1
kind: Service
metadata:
  name: {{ include "kagent.fullname" . }}-controller
  namespace: {{ include "kagent.namespace" . }}
  labels:
    {{- include "kagent.controller.labels" . | nindent 4 }}
  {{- with .Values.controller.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.controller.service.type }}
  ports:
    - port: {{ .Values.controller.service.ports.port }}
      targetPort: {{ .Values.controller.service.ports.targetPort }}
      protocol: TCP
      name: controller
    - port: {{ .Values.controller.service.ports.grpc }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "kagent.controller.selectorLabels" . | nindent 4 }}
